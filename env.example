# Configuración de la base de datos PostgreSQL
# Copia este archivo como .env y completa con tus credenciales

DB_HOST=localhost
DB_PORT=5432
DB_NAME=crud_productos
DB_USER=postgres
DB_PASSWORD=tu_contraseña_aqui

# Configuración de JWT
# IMPORTANTE: Cambia esta clave por una segura en producción
# Puedes generar una con: openssl rand -hex 32
JWT_SECRET_KEY=tu-secret-key-super-segura-cambiar-en-produccion-generar-con-openssl-rand-hex-32

